const point = {
    x: 5,
    y: 8,
}
        console.log("point est x = " + point.x + "point y est =" + point.y);

        point.x = 10;
        point.y = 12;
        console.log(`nouvelle point est x = ${point.x} y = ${point.y}`)
